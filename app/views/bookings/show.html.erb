<h1>You just booked this trip!</h1>
<div>
  <p>
    <%= @booking.trip.name %>
  </p>
  <p>
    <%= @booking.trip.start_date %>
  </p>

  <p>
    <%= @booking.trip.end_date %>
  </p>



</div>
<br>
<%= link_to 'Show Trips', trips_path(@trip), :class => "btn btn-primary" %>
<br>
<% if policy(@booking).destroy? %>
  <%= link_to "Delete booking", trip_booking_path(@booking, @trip), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}%>
<% end %>
